@page "/"
@rendermode InteractiveServer
@using Core

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app :3

<button class="btn btn-primary" @onclick="Login">TEST</button>

@code {

    [Inject] LoginService LoginService { get; set; }
    private void Login()
    {
        bool isValid = LoginService.ValidateUser("XD", "XDD");
        Console.WriteLine(isValid);
    }
}

